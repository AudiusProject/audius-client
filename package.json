{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "postinstall": "npm run bootstrap && lerna run build --scope=@audius/stems && npm run install-hooks",
    "install-hooks": "mookme init --only-hook --skip-types-selection"
  },
  "devDependencies": {
    "lerna": "5.0.0"
  },
  "dependencies": {
    "@escape.tech/mookme": "1.2.0"
  }
}